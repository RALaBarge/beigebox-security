# BeigeBox local overrides — copy to .env and edit for your machine
# cp .env.example .env

# Ports (host-side)
BEIGEBOX_PORT=1337
OLLAMA_PORT=11434
WEBUI_PORT=3000
WHISPER_PORT=9000
KOKORO_PORT=8880

# Mount your host Ollama model directory so the container shares your models
# avoids re-downloading — just point at your ~/.ollama
OLLAMA_DATA=/home/jinx/.ollama

# API keys (leave blank if not using Google search)
GOOGLE_API_KEY=
GOOGLE_CSE_ID=

# Open WebUI auth (false = no login required for local use)
WEBUI_AUTH=false

# Whisper model (swap for a larger one if you want better accuracy)
WHISPER_MODEL=Systran/faster-whisper-base
