# Local env overrides — machine-specific, never commit
docker/.env
docker/docker-compose.yml
docker/docker-compose.yaml
.env

# Local configs — copy from *.example.yaml, customize, never commit
config.yaml
runtime_config.yaml

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/

# Data
data/
*.db
*.jsonl
chroma/

# Agent workspace (input/output directory — keep .gitkeep placeholders only)
workspace/
!workspace/
!workspace/in/
!workspace/out/
!workspace/mounts/
!workspace/in/.gitkeep
!workspace/out/.gitkeep
!workspace/mounts/.gitkeep
!workspace/README.md

# Ingested RAG corpora — never commit (customer data + large)
workspace/out/rag/
workspace/in/*

# Customer-specific output files — case analysis, sentiment reports
workspace/out/ryans_*.json
workspace/out/ryans_*.md
workspace/out/ral_notes*
workspace/out/ryan_jason_sentiment.md
workspace/out/tap_macos_debug.md
workspace/out/commit_audit.md

# Customer-specific scripts — internal tooling, not for distribution
scripts/ryans_*.py
scripts/slack_ingest.py
scripts/sf_ingest_run.py
scripts/jira_ingest_run.py
scripts/confluence_ingest_run.py
scripts/ingest_to_chroma.py
scripts/browserbox_*.py

# Logs
logs/
*.log

# Internal security analysis / red team reports (not for distribution)
oss/

# Secrets
*.key
*.pem
*.p12
*.pfx

# Virtual environments
.venv/
venv/
env/

# Website source (separate deployment)
html_beigebox/

# Design docs / session notes — internal only
2600/
evals/hle.yaml

# Claude Code dev tooling — internal only
docker/Dockerfile.claude
scripts/claude-sandbox.sh

# Rust/WASM build artifacts
wasm_modules/*/target/

CLAUDE.md
.claude/
2600/skills/tone-ryan/
