"""
Backend plugins — custom inference engine integrations.

Drop a Python file here with a Backend class and it auto-registers.
BeigeBox auto-discovers and loads all plugins at startup via plugin_loader.py.
"""
