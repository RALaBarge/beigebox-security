# Local env overrides — machine-specific, never commit
docker/.env
docker/docker-compose.yml
docker/docker-compose.yaml
.env

# Local configs — copy from *.example.yaml, customize, never commit
config.yaml
runtime_config.yaml

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/

# Data
data/
*.db
*.jsonl
chroma/

# Agent workspace (input/output directory — keep .gitkeep placeholders only)
workspace/
!workspace/
!workspace/in/
!workspace/out/
!workspace/mounts/
!workspace/in/.gitkeep
!workspace/out/.gitkeep
!workspace/mounts/.gitkeep
!workspace/README.md

# Ingested RAG corpora — never commit (customer data + large)
workspace/out/rag/
workspace/in/*

# Logs
logs/
*.log

# Secrets
*.key
*.pem
*.p12
*.pfx

# Virtual environments
.venv/
venv/
env/

# Website source (separate deployment)
html_beigebox/

# Design docs / session notes — internal only
2600/
evals/hle.yaml

# Claude Code dev tooling — internal only
docker/Dockerfile.claude
scripts/claude-sandbox.sh

# Rust/WASM build artifacts
wasm_modules/*/target/

CLAUDE.md
.claude/
