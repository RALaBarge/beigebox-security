# Core
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
pyyaml>=6.0
pydantic>=2.4.0
pydantic-settings>=2.0
bcrypt>=4.0.0

# Storage (postgres via psycopg2-binary + pgvector)
# Removed: chromadb — fully replaced by postgres backend.

# Web scraping / search are now stdlib-only (html.parser + httpx).
# Removed: requests, beautifulsoup4, ddgs, lxml, itsdangerous, psutil, pynvml.

# Credentials (agentauth — OS keychain credential management for agent frameworks)
agentauth>=0.1.0

# WASM runtime
wasmtime

# Dev / Test
pytest>=8.0
pytest-asyncio>=0.24.0

# Security scanning tools (bandit, semgrep, pip-audit) are NOT in this
# runtime list — they are CI-only and live in the `security` dependency
# group in pyproject.toml. Install with: `uv sync --group security`
# or `pip install -e .[security]` (after extras-mapped install).
